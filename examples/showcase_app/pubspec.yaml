name: flutterlens_showcase_app
description: Deliberately inspectable showcase application for FlutterLens.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  flutterlens:
    path: ../../packages/flutterlens

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
